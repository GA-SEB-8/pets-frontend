import React from 'react'

const PetDeleteButton = ({petId}) => {
    console.log(petId)
    // Make the api call to delete the specific pet resource
  return (
    <button>Delete</button>
  )
}

export default PetDeleteButton
